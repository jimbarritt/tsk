# Local dev setup

## `.inbox/`

Untracked, gitignored directory at the repo root. Drop context files here
for Claude to process: notes, exports, drafts.

Also serves as a sync point for conversations exported from Claude
Desktop into this repo.
